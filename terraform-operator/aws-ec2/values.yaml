# 原配置不变，仅追加以下内容
approval:
  # 🔥 生产环境必须开启：plan后自动阻塞，等待人工审批
  enabled: true  

# 原有配置
aws:
  region: us-east-1
  credsSecret: aws-creds
ec2:
  name: aws-ec2
  instanceType: t3.micro
terraform:
  version: "1.4.6"